#pragma once
namespace Log
{
	// Empty function prototypes to be replaced with your implementations
	template <typename... T>
	void Debug(T ... args)
	{
	};

	template <typename... T>
	void Info(T ... args)
	{
	};

	template <typename... T>
	void Warning(T ... args)
	{
	};

	template <typename... T>
	void Error(T ... args)
	{
	};
}
